namespace TodoApp.Core.Enums;

/// <summary>Task priority levels — each can have a distinct notification sound.</summary>
public enum Priority
{
    Low      = 0,
    Medium   = 1,
    High     = 2,
    Critical = 3
}
